<h2>タスク詳細</h2>

<table>
  <tr>
    <th>タスク名</th>
    <th>内容</th>
    <th>期日</th>
    <th>作成者</th>
  </tr>

  <tr>
    <td><%= @task.title %></td>
    <td><%= @task.content %></td>
    <td><%= @task.deadline %></td>
    <td><%= @task.user.email %></td>
  </tr>
</table>

<%= link_to :新規登録, new_task_path %>
<%= link_to :タスク一覧, tasks_path %>
